Class Gaia.Flux { ClassMethod Go() [ Language = python ] { __import__("csv").writer(open("/home/irisowner/dev/data/out/challenge_output.csv","w",newline="")).writerows([["source_id","bp_min_flux","bp_max_flux","rp_min_flux","rp_max_flux","percentage_change"]]+sorted([[source["source_id"],*(bands[0] or ("","")),*(bands[1] or ("","")),change] for extremes in [lambda arr:(lambda valid: valid and (min(valid),max(valid)))([flux for flux in __import__("json").loads(arr or "[]") if flux and flux==flux and flux>0])] for path in __import__("glob").glob("/home/irisowner/dev/data/in/*.gz") for source in __import__("csv").DictReader(line for line in __import__("gzip").open(path,"rt") if line[0]!="#") for bands in [[extremes(source["bp_flux"]),extremes(source["rp_flux"])]] for change in [100*max((high/low-1 for low,high in filter(None,bands)),default=0)] if change>100],key=lambda row:-row[-1]))}}